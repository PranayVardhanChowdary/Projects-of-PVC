# AGENTS.md

## Cursor Cloud specific instructions

This repository (`Projects-of-PVC`) is currently a bare placeholder containing only a `README.md`.

- **No source code, dependencies, services, tests, or build system exist yet.**
- **No package manager lockfile or dependency manifest is present.**
- The update script is intentionally a no-op (`echo "No dependencies to install"`).
- When source code is added, update the VM environment script and this file accordingly.
